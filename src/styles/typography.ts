import styled from "styled-components";
import { BaseTitle } from "./components/typography";
import { iTextProps } from "./typesStyles/typeTitle";

export const StyledTitle = styled(BaseTitle)<iTextProps>`

//escrever aqui 

`