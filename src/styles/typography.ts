import styled from "styled-components";
import { BaseTitle } from "./components/typography";
import { iTextProps } from "./typesStyles/typeTypography";

export const StyledTitle = styled(BaseTitle)<iTextProps>`
  //escrever aqui
`;

export const StyledSpan = styled(BaseTitle)<iTextProps>`
  //escrever aqui
`;

export const StyledParagraph = styled(BaseTitle)<iTextProps>`
  //escrever aqui
`;